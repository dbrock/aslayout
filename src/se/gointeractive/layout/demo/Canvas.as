package se.gointeractive.layout.demo
{
  import se.gointeractive.layout.geometry.Rectangle;
  
  public interface Canvas
  { function drawRectangle(rectangle : Rectangle, color : uint) : void; }
}
